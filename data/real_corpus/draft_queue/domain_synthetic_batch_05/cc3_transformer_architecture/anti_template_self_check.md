# Anti-template self check

- worker_id: CC-3
- batch_id: domain_synthetic_batch_05
- strategy summary:
  - The batch uses an 8-topic hybrid instead of repeating one dense subtopic cluster.
  - Titles avoid the old lesson-number shell and instead reference a concrete scenario or artifact.
  - Writing forms rotate every 20 files and are mixed inside each wave so neighbors do not read alike.
  - Every manifest row carries a concrete_anchor and anti_template_note before file generation begins.
  - Markdown files vary between Q&A, bug diary, table, audit sheet, pseudo-run log, and config review shapes.
  - Python files print topic-specific artifacts such as mask rows, logits shapes, score grids, norm stats, or decode decisions.
  - Layer-norm and residual files were capped to avoid batch_04-style overconcentration.
