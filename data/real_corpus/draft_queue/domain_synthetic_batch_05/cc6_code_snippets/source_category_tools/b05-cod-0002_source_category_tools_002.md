---
draft_status: candidate
topic_id: B05-COD-0002
source_category: synthetic_examples
project_backbone: cs_ml_python_transformer_training_systems
worker_id: CC-6
approved_for_training: false
contains_external_text: false
contains_private_data: false
target_use: draft_review_only
batch_id: domain_synthetic_batch_05
---

# Why Unknown Categories Should Be Surfaced Early

Learning target: Use a small failure case to show why unknown source labels must be reported rather than silently folded in.
Writing form: debugging diary
Concrete anchor: failure scenario

The point of this draft is to leave behind one inspectable operational clue, not a generic review speech.

## First symptom

A small mismatch is enough for a real teaching point when the reader can follow the order of discovery.

## Tiny log

```text
read artifact
compare expectation
isolate mismatch
state next fix
```

The next check stays tied to `failure scenario` instead of drifting into generic review language.

Observation 35: keep the note specific to `source_category_tools` and tied to `failure scenario`.

Observation 37: keep the note specific to `source_category_tools` and tied to `failure scenario`.

Observation 39: keep the note specific to `source_category_tools` and tied to `failure scenario`.

Observation 41: keep the note specific to `source_category_tools` and tied to `failure scenario`.

Observation 43: keep the note specific to `source_category_tools` and tied to `failure scenario`.

Observation 45: keep the note specific to `source_category_tools` and tied to `failure scenario`.

Observation 47: keep the note specific to `source_category_tools` and tied to `failure scenario`.

Observation 49: keep the note specific to `source_category_tools` and tied to `failure scenario`.

Observation 51: keep the note specific to `source_category_tools` and tied to `failure scenario`.
