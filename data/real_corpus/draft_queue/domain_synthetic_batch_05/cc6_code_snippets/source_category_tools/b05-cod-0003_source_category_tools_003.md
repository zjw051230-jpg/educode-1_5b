---
draft_status: candidate
topic_id: B05-COD-0003
source_category: synthetic_examples
project_backbone: cs_ml_python_transformer_training_systems
worker_id: CC-6
approved_for_training: false
contains_external_text: false
contains_private_data: false
target_use: draft_review_only
batch_id: domain_synthetic_batch_05
---

# Comparing Train and Validation Source Mix

Learning target: Compare train and validation source mixes and identify a skew worth investigating.
Writing form: checklist
Concrete anchor: before/after comparison

The point of this draft is to leave behind one inspectable operational clue, not a generic review speech.

## Checklist

- Name the field that drives the decision.
- Point to one neighboring field that could contradict it.
- State the concrete consequence.
- Keep the example narrow enough to inspect manually.

## Slice

```text
synthetic_examples | 11 | train-heavy
external_general_text | 2 | validation-only
internal_notes | 1 | should stay visible
```

Observation 35: keep the note specific to `source_category_tools` and tied to `before/after comparison`.

Observation 37: keep the note specific to `source_category_tools` and tied to `before/after comparison`.

Observation 39: keep the note specific to `source_category_tools` and tied to `before/after comparison`.

Observation 41: keep the note specific to `source_category_tools` and tied to `before/after comparison`.

Observation 43: keep the note specific to `source_category_tools` and tied to `before/after comparison`.

Observation 45: keep the note specific to `source_category_tools` and tied to `before/after comparison`.

Observation 47: keep the note specific to `source_category_tools` and tied to `before/after comparison`.

Observation 49: keep the note specific to `source_category_tools` and tied to `before/after comparison`.

Observation 51: keep the note specific to `source_category_tools` and tied to `before/after comparison`.
